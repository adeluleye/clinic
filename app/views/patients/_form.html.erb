  <%= error_messages_for(@patient) %>

  <table summary="Patient form fields">
      <tr>
        <th>Name</th>
        <td><%= f.text_field(:name) %></td>
      </tr>

      <tr>
        <th>Address</th>
        <td><%= f.text_field(:address) %></td>
      </tr>

      <tr>
        <th>Age</th>
        <td><%= f.text_field(:age) %></td>
      </tr>

      <tr>
        <th>Diagnosis</th>
        <td><%= f.text_field(:diagnosis) %></td>
      </tr>

      <tr>
        <th>Status</th>
        <td><%= f.text_field(:status) %></td>
      </tr>

      <tr>
        <th>Ward Identity</th>
        <td><%= f.text_field(:ward_id) %></td>
      </tr>
  </table>
  